import React from 'react';
import { shallow } from 'enzyme';
import LibraryList from './container/libraryList';

describe("ComponentName", () => {
  it("should render my component", () => {
    const wrapper = shallow(<ComponentName />);
  });
});